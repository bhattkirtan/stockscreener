[Unit]
Description=Capital.com Trading Bot — M15 (GOLD 15-minute)
After=network-online.target
Wants=network-online.target

[Service]
Type=simple
User=botuser
WorkingDirectory=/home/botuser/stockScreener/cloud-function
EnvironmentFile=/home/botuser/stockScreener/cloud-function/.env.m15
ExecStart=/home/botuser/venv/bin/python scripts/trading_bot.py

# Auto-restart on crash (the bot also has its own reconnect loop for WS drops)
Restart=on-failure
RestartSec=15s

# Logs go to journald — view with: journalctl -u bot-m15 -f
StandardOutput=journal
StandardError=journal
SyslogIdentifier=bot-m15

[Install]
WantedBy=multi-user.target
